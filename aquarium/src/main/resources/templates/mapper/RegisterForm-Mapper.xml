<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camel.project.mapper.RegisterFormMapper">
    <insert id="insertMember" parameterType="com.camel.project.dto.RegisterForm">
        INSERT INTO member (memberNo, memberId, memberPw, memberEmail, memberName, memberBirth, memberPhone)
        VALUES(memberNo_seq.NEXTVAL, #{memberId}, #{memberPw}, #{memberEmail}, #{memberName}, #{memberBirth}, #{memberPhone})
    </insert>
    
   <select id="getId" resultType="com.camel.project.dto.RegisterForm" parameterType="String">
		select count(*) from member
		where memberId = #{memberId}
	</select>
</mapper>